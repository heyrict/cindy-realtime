/*
 * ScheduleAddForm Messages
 *
 * This contains all the text for the ScheduleAddForm component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.ScheduleAddForm';

export default defineMessages({
  scheduled: {
    id: `${scope}.scheduled`,
    defaultMessage: 'Scheduled Time',
  },
  content: {
    id: `${scope}.content`,
    defaultMessage: 'Content',
  },
});
